generator client {
provider = "prisma-client-js"
}

datasource db {
provider = "sqlite"
url      = env("DATABASE_URL")
}

model Tyre {
id        Int    @id @default(autoincrement())
name      String
category  String
speed     Int
grip      Int
imageUrl  String?
}
